{
  "compilerOptions": {
    // "strict": true,
  },
  "extends": "expo/tsconfig.base",
  "exclude": [
    "node_modules",
    "build",
    "scripts",
    "acceptance-tests",
    "webpack",
    "jest",
    "src/setupTests.ts"
  ]
}
